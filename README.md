# Duplex-Communication-Logger-Ethernet-UART-using-Python
This project implements full-duplex communication logging using Python for two communication interfaces-  Ethernet and communication  Two separate Python scripts were developed to handle duplex communication over each interface. The scripts continuously send and receive data simultaneously and automatically generate log files for both data.
